# Provenance: 2026-09-23 attempt-2 partial build (stopped)

- **Source commit built**: `7ac212c` (main at the time; includes the WAL fix #103).
- **Build**: `packaging/build_installer.ps1`, Nuitka 4.2.2, gcc 15.2.0 (`-O2`-patched),
  `--jobs=1 --low-memory`, `--include-package=numpy/scipy/...` with **no** test-suite exclusions.
  Started 10:12:34 IST, Python-level stage done and C compile started 10:34:12, stopped
  deliberately at ~15:12:45 IST (laptop closing) with 2,217 of 3,079 `.c` files compiled.
- **Machine state**: Defender exclusions active for `%LOCALAPPDATA%\Nuitka` and `packaging\`
  (verified by GG). Another session's heavy processes were running at start (see
  `docs/RESUME.md`). This is a contended run, not a clean benchmark.
- **ccache state**: warm only for what the killed attempt 1 compiled (382 hits of 2,217 invocations, matching ccache's own `direct_cache_hit` count; mostly PIL, docx,
  anyio, cryptography, click, datasketch). Every numpy/scipy/onnxruntime file was a miss, so the
  numpy/scipy numbers below are real gcc time.
- **Generated by**: `python scripts/nuitka_compile_breakdown.py packaging/build/entry_point.build
  --out-dir reports/build-timing/2026-09-23-attempt2-partial --stop-time 2026-09-23T15:12:45`,
  reading `entry_point.build/ccache-24252.txt` (per-invocation STARTED/DONE timestamps, µs
  resolution). The `.o`-mtime gap column agrees with the ccache `wall` column to 0.1 min for
  every package.
- **Files**: `summary.md` (per-package table), `per_file.csv` (one row per gcc invocation).

## What is measured vs. estimated

| Quantity | Value | Status |
|---|---:|---|
| C-compile wall, first 2,217 files | 279.6 min | MEASURED |
| of which numpy+scipy test modules (320 files) | 84.1 min | MEASURED |
| scipy share of measured wall | 80.1 min (28.6%) | MEASURED |
| numpy share of measured wall | 58.2 min (20.8%) | MEASURED |
| Remaining 862 files, not compiled | ~214 min | ESTIMATED: per-package measured s/MB of `.c` × remaining `.c` bytes; scipy remaining = 599 files / 646 MB of `.c`, ~188 min |
| Full cold C stage, no exclusions | ~494 min | measured 280 + estimated 214 |
| Allow-list removes | 604 `.c` files / 866 MB of generated C | MEASURED (file count/bytes) |
| Allow-list saving, cold | ~199 min (84 measured + 115 estimated) | PARTLY ESTIMATED |
| Full cold C stage with allow-list | ~294 min | ESTIMATED |

The estimate assumes compile time scales with generated-C bytes within a package, which held
per package in the measured portion. It is not a benchmark. The next real build's
`packaging/build/compile_breakdown/summary.md` replaces it.

The earlier "~150 of ~197 min is scipy" figure (W2/V4) was spot-sampled and never committed.
This measurement supersedes it. On this contended run, scipy was 28.6% of the first 280 minutes
and is projected at about 54% of a full cold C stage without exclusions (80 measured + ~188
estimated, of ~494).
